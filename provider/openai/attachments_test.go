package openai_test

import "testing"

// Verifies Chat and Responses deterministically map text, image, MP3, WAV, and
// PDF attachments into their expected provider request content.
func TestAttachmentMappingAcrossAPIs(t *testing.T) {
	t.Skip("TODO: implement deterministic attachment mapping coverage across both APIs")
}
